import SwiftUI

extension Color {
    static var bodyTextColor: Color {
        Color(light: .gray100, dark: .white)
    }

    static var newNoteBackgroundColor: Color {
       Color(studioColor: .spBlue50)
    }
}
